<div class="profile-wrap">
  <div class="row">
    <div class="col-md-8">
      <% provide(:title, @user.name) %>
      <h1>
        <% if @user.image? %>
          <%= image_tag @user.image.url %>
        <% else %>
          <%= gravatar_for @user %>
        <% end %>
        <%= @user.name %>
      </h1>
    </div>
  </div>
</div>