<% provide(:title, 'All users') %>
<h1>ユーザーの一覧</h1>
  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= image_tag user.image.url if user.image.present? %>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>